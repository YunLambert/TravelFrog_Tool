本工具试图独立出来，无论apkide反编译结果如何，均不影响此插件效果

将待反编译的APK拖入此目录，命名为org.apk，然后双击pl.bat运行即可

注意：请勿手动关闭cmd窗口，等待窗口自动关闭，即使出错，也可以继续正常运行

反编译出的jar，既可以用jdgui.exe打开，也可以用luten打开

两种各有优劣：
jdgui:可同时打开多个jar包，且有引用时，可以自动跳转到对应jar包，并支持中文，但jd-gui在反干扰混淆上不佳
luten:与jd-gui正好相反，一定抗干扰能力，但目前不支持中文，且只能打开单一jar包